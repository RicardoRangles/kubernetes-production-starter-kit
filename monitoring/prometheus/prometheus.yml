global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - alerts/*.yml

scrape_configs:

  - job_name: 'kubernetes-nodes'
    static_configs:
      - targets:
        - node-exporter:9100

  - job_name: 'kubernetes-pods'
    kubernetes_sd_configs:
      - role: pod
